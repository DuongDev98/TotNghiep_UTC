@model System.Data.DataTable
@{
    ViewBag.Title = "Danh sách mặt hàng nhập theo ngày";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<form action="" method="get">
    <div class="row">
        <div class="col-6">
            <div class="form-group">
                <div class="row">
                    <div class="col-2">
                        <p>Từ ngày: </p>
                    </div>
                    <div class="col-4">
                        <input type="date" class="form-control" name="fDate" value="@ViewBag.fDate">
                    </div>
                    <div class="col-2">
                        <p>Đến ngày: </p>
                    </div>
                    <div class="col-4">
                        <input type="date" class="form-control" name="tDate" value="@ViewBag.tDate">
                    </div>
                </div>
            </div>
        </div>
        <div class="col-2">
            <button type="submit" class="btn btn-info btnRefresh">Refresh</button>
        </div>
    </div>
</form>

<table class="table table-hover">
    <thead>
        <tr>
            <th scope="col">#</th>
            <th scope="col">Ngày</th>
            <th scope="col">Số phiếu</th>
            <th scope="col">Nhà cc</th>
            <th scope="col">Mã hàng</th>
            <th scope="col">Mặt hàng</th>
            <th scope="col">Đơn giá</th>
            <th scope="col">Số lượng</th>
            <th scope="col">Thành tiền</th>
        </tr>
    </thead>
    <tbody>
        @{
            if (Model != null && Model.Rows.Count > 0)
            {
                int i = 1;
                foreach (System.Data.DataRow row in Model.Rows)
                {
                    <tr>
                        <th scope="row">@i</th>
                        <td>@row["NGAY"]</td>
                        <td>@row["SOPHIEU"]</td>
                        <td>@row["DNHACUNGCAP_NAME"]</td>
                        <td>@row["CODE"]</td>
                        <td>@row["NAME"]</td>
                        <td>@row["DONGIA"]</td>
                        <td>@row["SOLUONG"]</td>
                        <td>@row["THANHTIEN"]</td>
                    </tr>
                    i++;
                }
            }
        }
    </tbody>
</table>